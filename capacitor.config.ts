import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.examenprogramacionhibrida',
  appName: 'examenprogramacionhibrida',
  webDir: 'www'
};

export default config;
